{
  "name": "lush-makeover-web",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "build": "npm run build --workspace frontend",
    "start": "npm start --workspace frontend"
  }
}
