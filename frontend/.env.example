# Copy this file to .env and adjust the values.

# URL of the backend API. Local development uses the FastAPI dev server;
# in production (Vercel) set this to your hosted backend URL.
REACT_APP_API_URL=http://localhost:8000
